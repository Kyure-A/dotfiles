# -*- mode: snippet -*-
# name: Mex 
# key: Mex
# --

signed Mex(set<int> seq)
{
  int seq_max = *seq.rbegin();

  for (int i = 0; i < seq_max; ++i)
    {
      if (seq.find(i) == seq.end()) return i;
    }

  return seq_max + 1;
}