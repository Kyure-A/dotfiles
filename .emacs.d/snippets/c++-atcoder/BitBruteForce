# -*- mode: snippet -*-
# name: bitBruteForce
# key: bitfor
# --

for (int bit = 0; bit < (1 << ${1:n}); ++bit)
  {
    for (int i = 0; i < ${1:n}; ++i)
      {
        if (bit & (1 << i))
      	  {
	    $0	
          }
      }
  }