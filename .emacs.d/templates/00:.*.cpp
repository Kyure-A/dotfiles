#include <bits/stdc++.h>
#define ctoi(char) int(char) - 48
#define rep(counter, range) for (int counter = 0; counter < (int)(range); counter++)
#define digit_sum(n, sum) for (sum = 0; 0 < n; sum += n % 10, n /= 10)
typedef long long ll;
using namespace std;

int main () {



}